<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.nwpu.i_gua_da.mapper.UserMapper">
    <resultMap id="User" type="org.nwpu.i_gua_da.entity.User">
        <id property="userId" column="user_id"/>
    </resultMap>

    <update id="setUserAsAdmin">
        UPDATE user_info SET permission = #{adminCode} WHERE user_id = #{userId}
    </update>

    <update id="setUserAsCommon">
        UPDATE user_info SET permission = #{commonCode} WHERE user_id = #{userId} and `name` != #{adminName}
    </update>

    <select id="selectUserById" resultMap="User">
        SELECT * FROM user_info where user_id = #{userId}
    </select>

    <select id="selectUserByName" resultMap="User">
        SELECT * FROM user_info where name = #{userName}
    </select>

    <update id="deleteUserById">
        UPDATE user_info SET status = #{isDeleteStatus} where user_id = #{userId}
    </update>
</mapper>